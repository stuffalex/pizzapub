# Limita a memoria alocada para o daemon do Gradle e processos paralelos
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m
# Desabilita builds paralelos para evitar estourar a RAM rodando dois Vites e o Java juntos
org.gradle.parallel=false
org.gradle.caching=true
org.gradle.vfs.watch=true
