// ============================================================
// Tipos gerados pelo Supabase CLI — M5
// ============================================================
// Execute no M5:
//   supabase gen types typescript --project-id [SEU-PROJECT-REF] > src/types/database.types.ts
//
// Exemplo de uso (com tipagem completa):
//   const { data } = await supabase.from('produtos').select('id, nome, preco')
// ============================================================

export type Database = {
  // Será gerado automaticamente pelo Supabase CLI no M5
}
